# Demo 垂直切片任务清单（2 周）

> 目标：2 周出可玩、可面试讲的 2.5D 回合制战斗垂直切片。完成度 > 复杂度。
> 战斗设计锁定见 `战斗设计.md`。本文件只拆任务 + 定 AI 落点。

## 一、已冻结规模（不扩）

- 3 角色、≤3 敌、卡池 8 张、状态 5 种、每角色 3 招（普攻/技能/终结技 = 9 招）。
- 3 关编排：群怪 / 单 boss / boss+爪牙。
- 平台：PC/手机均可，先 PC（Unity，编辑器内验收）。

## 二、内容明细（具体到条目）

### 3 角色（战/坦/辅三角，全都走 普攻回能量→技能耗能量→终结技耗大招）

| 定位 | 普攻 | 技能 | 终结技 |
|------|------|------|--------|
| 输出·剑士 | 单体物理（回能量） | 单体高伤（耗能量） | 全体 AOE（耗满大招） |
| 坦克·护卫 | 单体物理（回能量） | 护盾+嘲讽（耗能量） | 全队大护盾（耗满大招） |
| 辅助·术士 | 单体魔法（回能量） | 单体治疗+攻增（耗能量） | 全队提速/回能量（耗满大招） |

### 8 张卡（反制为主，效果全走配置数值，不硬编码）

1. 打断 ×2：移除目标本回合出手（无论速度），只对「本回合意图已亮」的怪生效。
2. 虚弱 ×2：降低目标本回合「最终总伤」X%（乘法，打在落地伤害上）。取整=去尾。例：意图高攻 66 × 0.75 = 49.5 → 49。
3. 降速 ×1：目标本回合速度 -50%（延后排序）。
4. 提速 ×1：我方单体本回合速度 +50%（提前排序）。
5. 护盾 ×1：我方单体护盾 N 点，挡一次伤害。
6. 攻增 ×1：我方单体本回合攻击 +X%。

### 5 种状态

虚弱（降总伤 X%，去尾）、护盾、攻增、减速、打断（本回合不出手）。

### 敌人意图表（跨单位预告，回合开始即亮）

| 敌人 | 血量/速度定位 | 意图序列（循环） |
|------|--------------|----------------|
| 杂兵（群怪） | 血薄 / 速中 | 普攻 → 蓄力重击（次回合高伤，可打断） |
| 爪牙（精英） | 血中 / 速快 | 快速单体强攻 → 自我攻增 → 连击 |
| 领主（boss） | 血厚 / 速慢 | 普攻 → AOE → 蓄力大招（高伤，强意图=存牌打断点） |

### 3 关编排（覆盖设计里的三种敌人组合）

- 关 1：3 杂兵（群怪，教「速度排序」）。
- 关 2：1 boss（单 boss，教「打断蓄力 / 存牌爆发」）。
- 关 3：1 boss + 1 爪牙（最复杂，教「多意图抉择」）。

## 三、AI 内容管线（差异化标签，推荐落点已定）

落点：**先 AI 生成敌人数值配置，再自动战斗模拟器微调参数（闭环）**。

理由：
- 只做「AI 写文案/写技能描述」= 弱，面试师一眼看穿无价值。
- 只做「AI 生成配置」= 一次性文本搬运，无闭环、不可验证。
- 闭环 = 提示词生成敌人配置 JSON → 游戏读配置 → 无渲染自动战斗模拟器跑 N 局产出胜率 → 胜率超区间则回写提示词调参。这条管线是简历上、面试里都讲得动的「AI 内容管线」，且复用同一套战斗逻辑（headless），成本低。

v0 上限收紧：只生成「敌人」一类配置（不碰技能/关卡/数值全量），3 个敌人模板参数化即可，不做强化学习、不做 LLM 在线进游戏。

## 四、2 周任务拆解（14 天，每天一验收）

### 第 1 周：战斗骨架可玩（临时素材打穿一局）

- [ ] D1：工程脚手架 + 数据驱动配置框架（Unity 项目 + ScriptableObject/JSON 加载角色/敌人/卡牌/状态）。关键：数值零硬编码，贴合已往「数据驱动弱解谜」叙事。
- [ ] D2：回合管理器：速度混排排序 + 同速 tie-break 我方先 + 行动队列。
- [ ] D3：角色三循环：9 招逻辑 + 能量/大招条。
- [ ] D4：敌人意图系统：意图枚举 + 亮意图 + 敌人行动结算。
- [ ] D5：卡牌系统：每回合抽 1 / 可存 / 可多出 + 8 张卡效果 + 打牌窗口。
- [ ] D6：状态系统：5 状态 + 统一结算顺序（buff/debuff 生效时点）。
- [ ] D7：串联合一：死亡结算 → 胜负判定 → 一局完整跑通。

### 第 2 周：内容 + AI 管线 + 打磨

- [ ] D8：3 角色数值配平 + 3 敌配置（杂兵/爪牙/领主）+ 3 关编排落表。
- [ ] D9：AI 管线 v0：提示词 → 敌人配置 JSON → 游戏能读、能进局。
- [ ] D10：自动战斗模拟器：headless 复跑同一战斗逻辑，N 局产出胜率，回写调参闭环。
- [ ] D11：意图表落地 + 存牌爆发点验证（boss 强意图→打断 联动是否成立）。
- [ ] D12：2.5D 表现最小可用：镜头 / 行动条 / 意图图标 / 手牌 UI。
- [ ] D13：打磨 + bug + 录屏 + 面试讲稿（一句话可讲性 + 架构图）。
- [ ] D14：缓冲 + 验收清单核对（见下）。

## 五、验收标准（完成定义 = 逐条可查）

1. 一局完整打穿：选 3 角色 → 遇 3 关敌人 → 打牌反制 → 速度排序结算 → 胜负结算。
2. 三系统自洽：意图 / 手牌 / 速度排序能联动（boss 强意图 → 打断牌 → 本回合不出手）。
3. 数据驱动：角色 / 敌人 / 卡牌 / 状态全部来自配置，无硬编码数值。
4. AI 管线 v0 能跑：一个提示词生成一份敌人配置 JSON，游戏读入可进局。
5. 自动战斗模拟器跑 N 局并输出胜率（= AI 调参闭环证据）。
6. 交付物：2–3 分钟录屏 + 一页架构图 + 30 秒口播。

## 六、面试讲稿 hook（先存这里，D13 展开）

- 一句话：速度混排决定秩序，意图制造压力，卡牌在回合开始改秩序/反制。
- 架构三件套：数据驱动配置 → 确定性战斗状态机 → 无渲染模拟器复用。
- AI 标签：AI 生成敌人配置 + 自动模拟验证平衡，回写调参（闭环，不是一次性生成）。